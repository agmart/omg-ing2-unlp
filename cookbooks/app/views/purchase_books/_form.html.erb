<%= form_for(@purchase_book) do |f| %>
  <% if @purchase_book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@purchase_book.errors.count, "error") %> prohibited this purchase_book from being saved:</h2>

      <ul>
      <% @purchase_book.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :book_id %><br>
    <%= f.number_field :book_id %>
  </div>
  <div class="field">
    <%= f.label :purchase_id %><br>
    <%= f.number_field :purchase_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

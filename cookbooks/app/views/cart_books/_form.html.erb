<%= form_for(@cart_book) do |f| %>
  <% if @cart_book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cart_book.errors.count, "error") %> prohibited this cart_book from being saved:</h2>

      <ul>
      <% @cart_book.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :book_id %><br>
    <%= f.number_field :book_id %>
  </div>
  <div class="field">
    <%= f.label :cart_id %><br>
    <%= f.number_field :cart_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
